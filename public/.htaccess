<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /poll-app/
  
  # Allow existing files and directories to be served
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]
</IfModule>
